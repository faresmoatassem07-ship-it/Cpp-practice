#include <iostream>
#include <string>
#include <cmath>
#include <stack>
using namespace std;

int FindMin(stack<int>s){
    int Min=s.top();
    
    while(!s.empty()){
        if(s.top()<min){
            Min=s.top();
        }
        s.pop();
    }
    return Min;

}

int FindMin(int<stack>&s){
    stack<int>temp;
    int Min = s.top();
    while(!s.empty()){
        temp.push(s.top());
        if(temp.top()<min){
            Min=temp.top();
        }
        s.pop();
    }
    while(!temp.empty()){
        s.push(temp.top());
        temp.pop();
    }
    return Min;
}

void printStack(stack<int>s){
    while(!s.empty()){
        cout << s.top() << endl;
        s.pop();
    }
}

int main() {
  stack<int>s
  s.push(5);
  s.push(2);
  s.push(8);
  s.push(1);
  s.push(10);
  
  
  cout << findMin(s) <<endl;
  printStack(s);
  
  
  
  
  
    return 0;
}
