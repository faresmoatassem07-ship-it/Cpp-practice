#include <iostream>
#include <cmath>
#include <string>
#include <stack>
#include <queue>
using namespace std;

class Person{
    protected:
    string Name;
    
    public:
    Person(){}
    Person(string N){
        Name = N;
    }
    virtual ~Person(){}
    virtual void Display(){ //not needed to be abstract
    cout<<"the Name is: " <<Name <<endl;
    }   
};

class Student : public Person{
    private:
    int ID;
    double Gpa;
    static int StudentCount; //total no. of student objects created
    
    public:
    Student(string Name,int I , double G):Person(Name){
        ID=I;
        Gpa=G;
        StudentCount++;
    }
    Student(){
        ID=0;
        Gpa=0;
    }
    static int get_Count(){
        return StudentCount;
    }
    void Display() override {
        Person::Display();
        cout<<"the ID is: " <<ID << endl;
        cout <<"the Gpa is: " <<Gpa <<endl;
    }
    bool operator>(Student other){
        if(Gpa>other.Gpa){
            return true;
        }
    return false;
    }
    int get_Gpa(){
        return Gpa;
    }
    int get_Name(){
        return Name;
    }
};
int Student::StudentCount=0;

void InsertionSort(Person* arr[] , int size){
    for(int i =0 ; i<size-1 ; i++){
        Student key=arr[i];
        int j = i-1;
        while(j>=0 && arr[j].get_Gpa()>arr[key].get_Gpa()){
            arr[j+1]=arr[j]//shift right
            j--; //keep searching left for any other possiblities
        }
        arr[j+1]=key
    }
}
//extra Name searching func and assume they are put alphabetically

int BinarySearch(Person* arr[] , int size , string key){    //key=name
int LowerB=0;
int UpperB=size-1;
    while(LowerB<=UpperB){
        int MidPoint=(LowerB+UpperB)/2;
        if(arr[Midpoint].get_Name()==key){
            return Midpoint;
        }
        else if(arr[Midpoint].get_Name()>key){
            UpperB=Midpoint-1;
        }
        else{
            LowerB=Lower+1;
        }
    }
    return -1;
}

void PrintStack(stack<Student>s){
    while(!s.empty()){
        cout << s.top().Display() <<endl;
        s.pop();
    }
}

void PrintQueue(Queue<Student>q){
    while(!q.empty()){
        cout << "| " <<q.front().Display() <<" |";
        q.pop();
    }
}



int main(){
    const int Fixedsize = 5;
    Person* students[5];
    students[0]=new Student("John",371,3.4);
    students[1]=new Student("Mike",014,2.4);
    students[2]=new Student("Bob",975,1.9);
    students[3]=new Student("Kevin",464,4);
    students[4]=new Student("Doue",842,3.1);


    InsertionSort(students,size);
    BinarySearch(students,FixedSize,"Bob")

    stack<Student>s;
    queue<Student>q;
    
    for(int i=0 ; i<Fixedsize ; i++){
        s.push(students[i]);
    }
    for(int i=0 ; i<Fixedsize ; i++){
        q.push(students[i]);
    }

    PrintStack(s);
    PrintQueue(q);
    
    
    return 0;
}

